syntax = "proto3";

package geo;

service Geo {
  rpc HandleNearby(NearbyRequest) returns (NearbyResponse) {}
}

message NearbyRequest {
  uint32 ave = 1;

  // float lat = 1;
  // float lon = 2;
}

message NearbyResponse { repeated string hotels = 1; }
