apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f containers+svcs.yaml
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: reservation-service
  name: reservation-service
spec:
  replicas: 4
  selector:
    matchLabels:
      io.kompose.service: reservation-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f containers+svcs.yaml
        kompose.version: 1.34.0 (cbf2835db)
      labels:
        io.kompose.service: reservation-service
    spec:
      containers:
        - image: vicsli/hotel_reservation:latest
          name: hotel-reservation
          # imagePullPolicy: Never
          ports:
            - containerPort: 8660
              protocol: TCP
          resources:
            limits:
              cpu: "1"
      restartPolicy: Always
