apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f containers+svcs.yaml
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: geo-service
  name: geo-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: geo-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f containers+svcs.yaml
        kompose.version: 1.34.0 (cbf2835db)
      labels:
        io.kompose.service: geo-service
    spec:
      containers:
        - image: vicsli/hotel_geo:latest
          name: hotel-geo
          #imagePullPolicy: Never
          ports:
            - containerPort: 8660
              protocol: TCP
          resources:
            limits:
              cpu: "4"
      restartPolicy: Always
