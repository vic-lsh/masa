apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f containers+svcs.yaml
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: rate-memcached
  name: rate-memcached
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: rate-memcached
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f containers+svcs.yaml
        kompose.version: 1.34.0 (cbf2835db)
      labels:
        io.kompose.service: rate-memcached
    spec:
      containers:
        - args:
            - --port
            - "11003"
            - --threads
            - "4"
          image: memcached:1.6
          name: rate-memcached
          ports:
            - containerPort: 11003
              protocol: TCP
          resources:
            limits:
              cpu: "4"
      restartPolicy: Always
