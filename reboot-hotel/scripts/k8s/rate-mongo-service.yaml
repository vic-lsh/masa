apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: rate-mongo
  name: rate-mongo
spec:
  ports:
    - name: "27003"
      port: 27003
      targetPort: 27003
  selector:
    io.kompose.service: rate-mongo
