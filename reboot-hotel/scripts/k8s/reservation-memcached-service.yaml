apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: reservation-memcached
  name: reservation-memcached
spec:
  ports:
    - name: "11005"
      port: 11005
      targetPort: 11005
  selector:
    io.kompose.service: reservation-memcached
