apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f containers+svcs.yaml
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: reservation-memcached
  name: reservation-memcached
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: reservation-memcached
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f containers+svcs.yaml
        kompose.version: 1.34.0 (cbf2835db)
      labels:
        io.kompose.service: reservation-memcached
    spec:
      containers:
        - args:
            - --port
            - "11005"
            - --threads
            - "4"
          image: memcached:1.6
          name: reservation-memcached
          ports:
            - containerPort: 11005
              protocol: TCP
          resources:
            limits:
              cpu: "4"
      restartPolicy: Always
