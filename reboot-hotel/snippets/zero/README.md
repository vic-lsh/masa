# README

## Run Experiments
```bash
./run.sh
```
The load gen config is defined in each snippet folder, such as `prio-global/gen_config.json`. The output files are within the same snippet folder.

## Plot Results
```bash
./plot.sh
```
It plots `goodput`, `throughput`, and `error`. The output images are within the same snippet folder.
