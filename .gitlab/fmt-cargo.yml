
cargo_fmt:
  stage: quick-checks
  variables:
    CI: "true"
  script:
    - rustup component add rustfmt
    - ./scripts/check_format.sh
  # Only run this job if Rust files have changed
  rules:
    - changes:
        - "**/*.rs"
        - "Cargo.toml"
        - "Cargo.lock"
