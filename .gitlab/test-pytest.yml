pytest:
  image: python:3.11
  stage: quick-checks
  before_script:
    - python --version
    - pip --version
    - pip install --upgrade pip
    - pip install pytest pytest-mock
  script:
    - pytest
