version: 1
name: masa-evaluation

studies:
  fixed_capacity_goodput:
    question: >
      How much goodput does each policy achieve under increasing offered load
      at fixed provisioned capacity?
    experiments:
      - experiments/fixed_capacity/trace_short_sequential_branching.yaml
