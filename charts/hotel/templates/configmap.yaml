{{- if .Values.configMaps.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hotel.fullname" . }}-config
  labels:
    {{- include "hotel.labels" . | nindent 4 }}
data:
  config.json: |-
{{ .Values.configMaps.hotelJson | indent 4 }}
  policy_params.json: |-
{{ .Values.configMaps.policyParamsJson | indent 4 }}
{{- end }}
