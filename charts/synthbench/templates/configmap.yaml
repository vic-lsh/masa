apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "synthbench.fullname" . }}-config
  labels:
    {{- include "synthbench.labels" . | nindent 4 }}
data:
  config.json: |
    {{- toJson .Values.appConfig | nindent 4 }}
