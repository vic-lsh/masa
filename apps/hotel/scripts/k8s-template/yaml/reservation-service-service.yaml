apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: reservation-service
  name: reservation-service
spec:
  clusterIP: None
  ports:
    - name: "8660"
      port: 8660
      targetPort: 8660
  selector:
    io.kompose.service: reservation-service
