[package]
name = "multiqueue-prio-queue"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false
description = "k-relaxed concurrent priority queue using independent sub-heaps"
keywords = ["concurrent", "priority-queue", "multiqueue", "relaxed", "thread-safe"]
categories = ["concurrency", "data-structures"]

[dependencies]
rand = "0.8"
